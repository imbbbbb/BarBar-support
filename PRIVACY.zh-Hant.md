# BarBar 隱私權政策

最後更新：2026-09-20

[简体中文](PRIVACY.md) · **繁體中文** · [English](PRIVACY.en.md)

一句話版本：**BarBar 不收集你的任何資訊，也沒有伺服器可以收。**

下面把細節寫清楚，因為 BarBar 會向系統要兩個聽起來很嚇人的權限。

## BarBar 不做的事

- 不收集、不上傳、不儲存任何個人資訊
- 沒有帳號、沒有登入、沒有統計、沒有當機回報、沒有廣告、沒有第三方 SDK
- 不讀取你的檔案、剪貼簿、瀏覽紀錄、鍵盤輸入
- 不記錄你用了哪些 App、什麼時候用的

BarBar 沒有後端。開發者拿不到任何關於你的資料，因為根本沒有任何資料流向任何地方。

## 那兩個權限是做什麼的

### 輔助使用（Accessibility）

用來**知道選單列上有什麼**。

macOS 沒有公開介面能回答「選單列上現在有哪些圖像、分別屬於誰」。唯一的辦法是用輔助使用介面去列舉系統選單列上的狀態項目，讀出每個圖像所屬 App 的 bundle identifier 和它在螢幕上的位置。

BarBar 只讀這兩樣東西。它不讀視窗內容、不讀文字、不監聽鍵盤、不模擬輸入。

沒有這個權限，BarBar 的圖像清單會是空的。

### 螢幕錄製（Screen Recording）

用來**把圖像畫出來**。

在「下方面板」這種展開形式裡，被隱藏的圖像要重新畫在一個自己的面板上。選單列圖像的樣子只有系統知道（很多是動態的，例如電量百分比、網路速度），拿不到原始素材，只能照著選單列對應的那一小塊區域擷取一次畫面。

BarBar 擷取的範圍就是選單列上那幾十乘幾十個點，不擷取視窗、不錄影、不儲存。擷取到的點陣圖只存在記憶體裡，行程結束就沒了，不寫入磁碟。

沒有這個權限，面板裡只能顯示 App 自己的程式圖像，樣子不如本尊準確，但功能照常。

### 隱藏圖像本身不需要任何權限

這一點值得單獨說：BarBar 的核心功能——把圖像藏起來——走的是系統自己的選單列可見性機制，不需要上面任何一個權限。兩個權限都只服務於「看得見、認得出」。

你隨時可以在「系統設定 → 隱私權與安全性」裡收回它們。

## 唯一的對外連線

BarBar 每天會檢查一次有沒有新版本。這次請求送往：

- `https://imbbbbb.github.io/BarBar-support/appcast.xml`（更新資訊）
- `https://github.com/imbbbbb/BarBar-support/releases/...`（真的有新版本時才下載）

請求裡帶的是 BarBar 的版本號和你的 macOS 版本，用來判斷這個版本適不適合你。沒有任何識別碼、沒有任何能認出是你的東西。

GitHub 作為伺服器會像對待任何一次網頁存取那樣看到你的 IP 位址，這一點 BarBar 管不了，也和其他任何下載行為沒有區別。

你可以在「設定 → 一般 → 更新」裡關掉自動檢查。關掉之後 BarBar 不會主動連線任何網路位址。

## 你的設定存在哪

分組、順序、快速鍵這些設定存在本機的標準偏好設定裡（`~/Library/Preferences/com.barbar.app.plist`），不同步、不上傳。刪掉 BarBar 之後想徹底清乾淨，可以在終端機裡執行：

```
defaults delete com.barbar.app
```

## 診斷資訊匯出

「設定 → 關於 → 匯出診斷資訊」會產生一個文字檔，方便你貼進回報裡。

它是**你主動點了才產生、產生之後你先看得到、再由你決定貼不貼**的。裡面只有版本號、機型、權限狀態和選單列上各個 App 的 bundle identifier，沒有視窗標題、沒有截圖、沒有帶使用者名稱的檔案路徑。BarBar 不會自動傳送它。

## 變更

這份政策如果有變化，會隨新版本一起更新，並寫在發佈說明裡。

## 聯絡

有疑問請開 issue：<https://github.com/imbbbbb/BarBar-support/issues>
