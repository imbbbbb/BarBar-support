# BarBar 隐私政策

最后更新：2026-09-20

**简体中文** · [繁體中文](PRIVACY.zh-Hant.md) · [English](PRIVACY.en.md)

一句话版本：**BarBar 不收集你的任何信息，也没有服务器可以收。**

下面把细节写清楚，因为 BarBar 会向系统要两个听起来很吓人的权限。

## BarBar 不做的事

- 不收集、不上传、不存储任何个人信息
- 没有账号、没有登录、没有统计、没有崩溃上报、没有广告、没有第三方 SDK
- 不读取你的文件、剪贴板、浏览记录、键盘输入
- 不记录你用了哪些 app、什么时候用的

BarBar 没有后端。开发者拿不到任何关于你的数据，因为根本没有任何数据流向任何地方。

## 那两个权限是干什么的

### 辅助功能（Accessibility）

用来**知道菜单栏上有什么**。

macOS 没有公开接口能回答「菜单栏上现在有哪些图标、分别属于谁」。唯一的办法是用辅助功能接口去枚举系统菜单栏上的状态项，读出每个图标所属 app 的 bundle identifier 和它在屏幕上的位置。

BarBar 只读这两样东西。它不读窗口内容、不读文本、不监听键盘、不模拟输入。

没有这个权限，BarBar 的图标列表会是空的。

### 屏幕录制（Screen Recording）

用来**把图标画出来**。

在「下方面板」这种展开形态里，被隐藏的图标要重新画在一个自己的面板上。菜单栏图标的样子只有系统知道（很多是动态的，比如电量百分比、网速），拿不到原始素材，只能照着菜单栏对应的那一小块区域截一次图。

BarBar 截取的范围就是菜单栏上那几十乘几十个点，不截取窗口、不录屏、不保存。抓到的位图只存在内存里，进程退出就没了，不写入磁盘。

没有这个权限，面板里只能显示 app 自己的程序图标，样子不如真身准确，但功能照常。

### 隐藏图标本身不需要任何权限

这一点值得单独说：BarBar 的核心功能——把图标藏起来——走的是系统自己的菜单栏可见性机制，不需要上面任何一个权限。两个权限都只服务于「看得见、认得出」。

你随时可以在「系统设置 → 隐私与安全性」里收回它们。

## 唯一的对外连接

BarBar 每天会检查一次有没有新版本。这次请求发往：

- `https://imbbbbb.github.io/BarBar-support/appcast.xml`（更新信息）
- `https://github.com/imbbbbb/BarBar-support/releases/...`（真的有新版本时才下载）

请求里带的是 BarBar 的版本号和你的 macOS 版本，用来判断这个版本适不适合你。没有任何标识符、没有任何能认出是你的东西。

GitHub 作为服务器会像对待任何一次网页访问那样看到你的 IP 地址，这一点 BarBar 管不了，也和其他任何下载行为没有区别。

你可以在「设置 → 通用 → 更新」里关掉自动检查。关掉之后 BarBar 不会主动连接任何网络地址。

## 你的设置存在哪

分组、顺序、快捷键这些配置存在本机的标准偏好设置里（`~/Library/Preferences/com.barbar.app.plist`），不同步、不上传。删掉 BarBar 之后想彻底清干净，可以在终端里执行：

```
defaults delete com.barbar.app
```

## 诊断信息导出

「设置 → 关于 → 导出诊断信息」会生成一个文本文件，方便你贴进反馈里。

它是**你主动点了才生成、生成之后你先看得到、再由你决定贴不贴**的。里面只有版本号、机型、权限状态和菜单栏上各个 app 的 bundle identifier，没有窗口标题、没有截图、没有带用户名的文件路径。BarBar 不会自动发送它。

## 变更

这份政策如果有变化，会随新版本一起更新，并写在发布说明里。

## 联系

有疑问请开 issue：<https://github.com/imbbbbb/BarBar-support/issues>
